---
"@willow-ds/tokens": patch
---

Ship the built token artifacts (`dist/tokens.js`, `dist/tokens.css`, `dist/tokens.d.ts`) in the published package. Consumers installing `@willow-ds/tokens` now receive the CSS and JS token outputs via `@willow-ds/tokens` and `@willow-ds/tokens/css`.
