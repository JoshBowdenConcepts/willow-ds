---
"@willow-ds/tokens": minor
---

Emit real CSS custom properties from the token build. Import `@willow-ds/tokens/css` to get a `:root` base layer plus scoped overrides via `data-willow-*` attributes and breakpoint media queries.
